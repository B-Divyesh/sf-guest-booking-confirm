[
  {
    "id": "demo-confirmation-trail",
    "claim": "Try a guest request, owner approval, and confirmation without saving anything.",
    "where": "cold landing page, README",
    "test": "npm run test:e2e -- --grep @claim:demo-confirmation-trail",
    "sandbox": "fresh browser context at /demo; sample booking is owner-approved, guest confirms it, and the confirmed state is visible"
  },
  {
    "id": "demo-local-only",
    "claim": "Demo data stays in this browser.",
    "where": "cold landing page, demo banner, README",
    "test": "npm run test:e2e -- --grep @claim:demo-local-only",
    "sandbox": "fresh browser context at /demo; confirm sample booking; assert no API request and only a demo: localStorage key is written"
  },
  {
    "id": "no-tracking-cookies",
    "claim": "No tracking cookies.",
    "where": "site footer",
    "test": "npm run test:e2e -- --grep @claim:no-tracking-cookies",
    "sandbox": "fresh browser context through the demo confirmation flow; assert document.cookie is empty and every request stays same-origin"
  },
  {
    "id": "guest-no-account",
    "claim": "Guests need no account.",
    "where": "landing page, README",
    "test": "npm run test:e2e -- --grep @claim:guest-no-account",
    "sandbox": "fresh browser context; use the guest request form without a sign-in, then complete the private-link confirmation trail"
  },
  {
    "id": "owner-approval-before-booking",
    "claim": "Owners approve requests before booking.",
    "where": "landing page, guest request page, README",
    "test": "npm run test:e2e -- --grep @claim:owner-approval-before-booking",
    "sandbox": "fresh browser context; create a request, assert its pending state, approve it as the owner, then expose the guest confirmation state"
  },
  {
    "id": "guest-rescheduling",
    "claim": "Guests can use their private booking link to request another available time.",
    "where": "README, private booking page, demo",
    "test": "npm run test:e2e -- --grep @claim:guest-rescheduling",
    "sandbox": "fresh browser context at /demo; request the next sample time and assert the changed appointment card and waiting-for-approval state"
  },
  {
    "id": "guest-cancellation",
    "claim": "Guests can use their private booking link to cancel a booking.",
    "where": "README, private booking page, demo",
    "test": "npm run test:e2e -- --grep @claim:guest-cancellation",
    "sandbox": "fresh browser context at /demo; cancel the approved sample booking and assert its closed cancellation state"
  },
  {
    "id": "confirmed-calendar-ics",
    "claim": "A confirmed booking downloads an ICS calendar file with its appointment details.",
    "where": "README, private booking page, demo",
    "test": "npm run test:e2e -- --grep @claim:confirmed-calendar-ics",
    "sandbox": "fresh browser context at /demo; confirm the sample, trigger its calendar download, and assert filename plus VCALENDAR, summary, status, start, and end fields"
  },
  {
    "id": "manual-reminder-checklist",
    "claim": "Owners can record that a manual reminder was sent without the product sending a message.",
    "where": "README, owner panel, demo",
    "test": "npm run test:e2e -- --grep @claim:manual-reminder-checklist",
    "sandbox": "fresh browser context at /demo; begin request recording after load, mark the sample reminder sent, assert zero action requests, reload, and assert only same-origin GETs plus persisted state"
  },
  {
    "id": "free-desk-capacity-and-retention",
    "claim": "The free desk allows 30 active future bookings and deletes closed records after 30 days.",
    "where": "owner panel, privacy page, README",
    "test": "cargo test --locked claim_free_desk_capacity_and_retention",
    "sandbox": "temporary SQLite desk; create 29 active requests, submit 12 requests concurrently, assert exactly one succeeds and the final count is 30, then test 29/31-day cleanup"
  },
  {
    "id": "panel-pro-capacity-and-retention",
    "claim": "Panel Pro unlocks unlimited active bookings and 365-day closed-record retention.",
    "where": "owner panel, privacy page, README",
    "test": "cargo test --locked claim_panel_pro_capacity_and_retention",
    "sandbox": "temporary paid SQLite desk; create 31 active requests, then run cleanup over closed records aged 364 and 366 days"
  },
  {
    "id": "api-rate-limit",
    "claim": "Read API calls allow 40 requests per client in each one-second window; later calls return 429 with Retry-After: 1.",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:api-rate-limit",
    "sandbox": "fresh server and browser context; make 41 concurrent same-client HTTP reads and assert exactly one 429 response with Retry-After: 1; the Rust suite separately proves the shared SQLite counter is atomic"
  },
  {
    "id": "owner-entra-identity",
    "claim": "Owners sign in through Sociobot Microsoft Entra External ID; the product has no owner password.",
    "where": "owner panel, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:owner-entra-identity",
    "sandbox": "fresh browser context; assert the Sociobot Entra sign-in screen has no password, unauthenticated setup is rejected, then use the test-only identity gate to reach setup"
  },
  {
    "id": "container-runtime-contract",
    "claim": "The container contract sets UID 10001, PORT 8080, SQLite under /data, and graceful SIGTERM handling.",
    "where": "README",
    "test": "cargo test --locked claim_container_runtime_contract",
    "sandbox": "compile-time contract test against the shipped Dockerfile and Rust server entry point"
  }
]
